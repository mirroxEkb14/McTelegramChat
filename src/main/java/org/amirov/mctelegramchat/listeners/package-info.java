/**
 * This {@code listeners} package stores all the listeners that monitor events in Minecraft. Each class represents only
 * one listener that monitors only on type of events.
 *
 * @author zea1ot
 */
package org.amirov.mctelegramchat.listeners;